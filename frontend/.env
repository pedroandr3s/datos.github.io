# Deshabilitar modo CI para evitar que ESLint falle el build
CI=false

# Deshabilitar source maps para builds más rápidos
GENERATE_SOURCEMAP=false

# Deshabilitar ESLint completamente durante el build
DISABLE_ESLINT_PLUGIN=true

# Opcional: Deshabilitar warnings de ESLint
ESLint_NO_DEV_ERRORS=true