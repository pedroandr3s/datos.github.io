# Disable CI mode to prevent ESLint errors from failing the build
CI=false

# Disable source maps generation for faster builds
GENERATE_SOURCEMAP=false

# Optional: Disable ESLint entirely during build (not recommended for production)
# DISABLE_ESLINT_PLUGIN=true